//Este file contiene las conexiones/ relaciones entre bases de datos(modelos): uno a uno, muchos a muchos, uno a muchos
const Movies = require('./movies.models')

const initModels = () => {
  Movies
}

module.exports = initModels